{
  "summary": "A poster-scale wordmark interlocked with a photographic vinyl record on a two-colour achromatic ground, with monospaced technical metadata pinned to every edge. Scroll turns the record itself rather than decorative motion, and the page is complete and readable before any of it runs.",
  "style": {
    "description": "Cool achromatic ground with near-black ink and exactly one saturated signal accent used for emphasis and never as a fill. Display type is a heavy grotesk at poster scale with tight negative tracking; everything that is not the headline is small monospaced metadata with wide positive tracking. Depth comes from real photography and hairlines, never from shadows, gradient glows or cards.",
    "prompt": "Apply an achromatic technical-product style. Palette: ground #EFEFEE, a dimmer stage ground #E4E4E2, ink #0D0D0F, secondary ink #43444A, muted #6E6F76, one signal accent #2F5BFF plus #7C97FF as its lift on dark grounds, hairlines rgba(13,13,15,.12). Typography: display uses 'Archivo' 700-900 with letter-spacing -0.04em to -0.055em and line-height 0.74 to 0.98; metadata and labels use 'IBM Plex Mono' 400-500 at 10-12px with letter-spacing +0.14em to +0.16em, uppercase. Easing cubic-bezier(.16,1,.3,1) at 0.85-1.25s. Border-radius 0 everywhere except pill buttons. MUST commit to exactly two colours plus one accent, and the accent may only appear as small type, a dot, a rule or a single trace, never as a fill or a large area. MUST keep a constant layer of small monospaced metadata pinned to corners and edges: it is the counterweight that makes poster type read as design rather than as a banner. DO NOT use a hero banner, a gradient glow band, a dot or line grid backdrop, a bento grid, card rows, or drop shadows on anything except the cut-out subject."
  },
  "layout_and_structure": {
    "description": "Seven full-width sections that alternate ground colour. The hero pins a poster word to the bottom edge and crops it left and right against the frame, with the vinyl record low and wide across the right half so it physically overlaps the word. Body sections are two-column with a hairline-ruled data list or a to-scale technical diagram of the conversion pipeline. The page must genuinely scroll: a single 100vh hero with overflow hidden is an incomplete landing, not a design.",
    "prompts": [
      {
        "part": "Navigation",
        "prompt": "Fixed bar 64px tall, background the page ground at 82% with a 14px backdrop-blur and a 1px bottom hairline. Left: brand 'plx' in display 800 at 16px, tracking -0.035em, followed by a period in the signal accent. Right: five mono links at 11px, tracking 0.14em, uppercase — OVERVIEW, CONVERT, MATCH, SPEC, INSTALL — each with a transparent 1px bottom border that becomes the accent on hover, then a pill button labelled 'npm i -g plx'."
      },
      {
        "part": "Hero",
        "prompt": "Full viewport minus the nav, on the dimmer ground, with overflow:clip and isolation:isolate. Left column holds a mono eyebrow 'SPOTIFY ⇄ DEEZER', a display headline at clamp(34px,5.1vw,74px) reading 'Move your playlist.' with the final phrase 'No dev account.' in the accent, a 38ch lede — 'plx rebuilds playlists between Spotify and Deezer from browser sessions, not API keys. Read Spotify anonymously, write Deezer from one ARL cookie — no Premium, no OAuth, no Developer account.' — two pill buttons 'npm install -g plx' and 'Try --dry-run', then a hairline-topped block of three mono spec lines: 'READ — Spotify · anonymous', 'WRITE — Deezer · one ARL cookie', 'MATCH — 3 tiers · ±3s · whitespace-insensitive'. Keep every anchored element in the LEFT column: the record is deliberately oversized and crosses the entire right half, so anything pinned top-right collides with it. Position the record absolutely at bottom-right, width min(58vw,940px), bleeding about 4vw past the right edge, with drop-shadow(0 26px 34px rgba(13,13,15,.20)), and sit it low enough that its lower body genuinely overlaps the word."
      },
      {
        "part": "Inverted proof section",
        "prompt": "Full-bleed ink ground. Two columns: left holds a display headline 'Free text beats field syntax.', a lede, one enormous figure '19/19' at clamp(56px,9vw,132px) with tabular numerals and a small mono unit 'TRACKS' baseline-aligned beside it, then a mono legend. Right holds an SVG of two line traces drawn to the same scale, a faint reference that wanders (field syntax, which matched 9/19 on the same playlist) and an accent-coloured one that holds nearly flat (free text, 19/19), so the comparison IS the claim rather than an illustration of it. Generate both paths in JS from summed sine terms so the amplitudes stay honest to the numbers quoted."
      },
      {
        "part": "Technical diagram",
        "prompt": "Two columns: a display headline with a lede and a hairline-ruled definition list (mono label in the accent, plain description, mono value right-aligned, 1px top border per row) of the modules — spotify.ts / deezer.ts / converter.ts / matcher.ts. Beside it an SVG line drawing on the page ground of the conversion pipeline drawn strictly to scale in real units: CLI args → Spotify read (Pathfinder → embed fallback) → tiered match → Deezer write → CSV report, with hairline construction geometry, accent-coloured points of interest at the three match tiers (exact, fuzzy-duration, fuzzy-title), thin leader lines out to mono labels, and the pipeline path that draws itself in when the figure enters the viewport. Compute the geometry rather than eyeballing it, and give the drawing's own line a heavier stroke than the label leaders so it does not read as one more leader."
      },
      {
        "part": "Spec table",
        "prompt": "Two balanced columns of hairline-ruled rows, plain label left and mono tabular value right, around twelve rows — Language TypeScript · Runtime Node ≥ 22 · Install npm i -g plx · Direction Spotify ⇄ Deezer (reverse soon) · Credentials 1 (DEEZER_ARL) · Spotify read anonymous · Deezer write ARL cookie · Match tiers 3 · Duration match ±3s · Units ms → s · Artist match whitespace-insensitive · Write dedupe existing + in-batch · Embed fallback 100 tracks · Report CSV. No borders except the 1px row rules."
      },
      {
        "part": "Close",
        "prompt": "Display headline with a mono fine-print line, buttons pushed to the opposite edge, a hairline-topped footer strip of four mono items with the last pushed right, then the brand word 'plx' repeated full-width at clamp(84px,19.5vw,300px) and translated down 0.19em so the page's bottom edge crops it, bookending the hero."
      }
    ]
  },
  "special_ui_components": [
    {
      "component": "Type and record occlusion weave",
      "description": "The signature move: a poster-scale wordmark that the vinyl record passes through, some letters behind it and one in front.",
      "prompt": "Set the brand word 'plx' at clamp(88px,20.5vw,304px), weight 900, line-height 0.74, tracking -0.055em, as a flex row with justify-content:space-between spanning 105% of the width so the frame crops the first and last letters, translated down 0.10em so the bottom edge crops the baseline. CRITICAL: render the word TWICE as two absolutely positioned layers that overlay exactly, a back layer at z-index 1 and a front layer at z-index 3, with the record at z-index 2 between them. Each layer contains every letter and sets visibility:hidden on the ones it does not own, which keeps the flex metrics identical so the two layers register perfectly. Put 'p' and 'l' in the back layer and exactly ONE overlapping letter — 'x' — in the front layer. DO NOT try this with per-letter z-index inside a single element: the word needs a transform for the baseline crop and again for the scroll parallax, a transform creates a stacking context on its own, z-index:auto does not save you, and the letters' z-index is then trapped at the word's level so every overlapping letter falls behind the record and the effect silently does not exist."
    },
    {
      "component": "Hero record artwork",
      "description": "How to produce the cut-out vinyl record the occlusion depends on. A rectangular photo or a CSS circle collapses the whole style.",
      "prompt": "Generate the record as a photoreal studio product shot against a FLAT SATURATED CHROMA-GREEN backdrop, not white. Ask for the record to stay strictly neutral in colour, lit by soft directional light from the upper left with a smooth tonal gradient across its surfaces and one crisp specular line along its longest edge, placed in the right half of the frame with generous empty space to its left, and state that nothing else appears anywhere in the frame. Describe lighting as a RESULT ('soft directional light falls from the upper left'), never as equipment ('lit by a softbox'), because naming the gear makes the model draw the gear into the picture. Then key it: alpha = 1 - smoothstep(0.10, 0.30, (G - max(R,B)) / G). Normalise greenness by brightness rather than using a raw channel difference, or shadowed backdrop lands mid-matte and survives as a ragged grey band hugging the object. Despill by clamping G down to max(R,B) wherever it leads. Keep only the largest connected blob, since generators often leave a detached ghost of a part floating in the empty half of the frame. Trim to the alpha bounding box before use, or CSS positions the empty frame instead of the object and the type and record silently never overlap. A white sweep is NOT a substitute: a bright part of the record can be genuinely as bright as the backdrop it touches, and no threshold separates two regions that share a value. Because a record is a large reflective disc, chroma keying also fails — it bounces green across its own face — so take the disc's alpha from fitted geometry and use the key only to find its bounding box. Set both width and height attributes to the FINAL file's real pixels and add height:auto in CSS, since a stale height attribute silently stretches the record into an ellipse."
    },
    {
      "component": "Pinned counter-travel record stage",
      "description": "A sticky stage where the record turns with the scroll while a giant word slides the opposite way behind it.",
      "prompt": "A section 320vh tall containing a position:sticky, 100svh stage. Centre the vinyl record at width min(56vw,600px) at z-index 2. Behind it at z-index 1 put a single-line marquee of the two-word title 'SPOTIFY DEEZER' repeated three times at clamp(78px,17vw,250px), weight 900, opacity 0.14, white-space:nowrap. Map progress through the section to BOTH a rotation on the record and an opposing horizontal translation on the marquee so they counter-travel. Pin the section label bottom-left and a live mono readout bottom-right, and drive the readout from the same progress value so the number is telling the truth about the rotation."
    },
    {
      "component": "Runtime match-tier picker",
      "description": "A variant picker that repaints the entire section field rather than swatching a small preview.",
      "prompt": "A section whose background-color and color transition over 0.7s. A row of pill buttons — EXACT, FUZZY-DURATION, FUZZY-TITLE — each carrying data attributes for a ground, an ink and the values it reveals; clicking one repaints the whole section field and rewrites a three-cell definition panel below it describing how that tier matches: normalized title + first artist (whitespace-insensitive) + duration within ±3s / title + artist equal with duration ignored / one normalized title contains the other + artist equal. The panel's dividers MUST be borders coloured with color-mix(in srgb, currentColor 24%, transparent), never a coloured grid showing through 1px gaps: the gap technique needs the cells to paint the section ground, which is being swapped at runtime, so background:inherit resolves to the grid's own ink and the panel fills in solid over its own text. For the same reason nothing in this section may hold a fixed ink colour, so tint currentColor instead of naming a grey, or the body copy drops to about 1.8:1 on the dark options."
    },
    {
      "component": "Scroll choreography",
      "description": "Motion as an opt-in enhancement layered over a page that is already complete without it.",
      "prompt": "Add a 'js' class to the document element ONLY after checking that prefers-reduced-motion is not 'reduce', and scope every reveal rule to that class, so the reduced-motion render and the no-JS render are simply the finished page. Reveal on scroll with one IntersectionObserver (rootMargin '0px 0px -12% 0px', threshold 0.08) that adds an 'in' class and unobserves. Anything already on screen at load, such as the hero word and record, must reveal on requestAnimationFrame instead and must NOT be observed: on a short viewport it can sit inside the negative bottom margin, never intersect, and stay invisible permanently, which loses the hero's whole signature move on phones. Drive scroll effects from a single passive, rAF-throttled scroll listener. In the hero, translate the word up slightly and the record down slightly as you leave so they part. Express static emphasis by tinting the colour, never by setting element opacity, so opacity stays a reliable signal of whether a node has revealed. DO NOT animate anything that is not the product's own behaviour."
    },
    {
      "component": "Evidence rules",
      "description": "What the page is allowed to claim.",
      "prompt": "The only claims on the page are plx's own measured specifications for the shipped Spotify → Deezer direction: the 19/19 versus 9/19 search result, the ±3s duration tolerance, the ms→s duration-unit conversion, the 100-track embed cap, whitespace-insensitive artist matching, and write-time dedupe (existing-track skip + in-batch duplicate removal). The reverse direction (Deezer → Spotify) is in progress and must be signalled only as 'reverse soon' or 'planned', never presented as shipping. DO NOT invent testimonials, press logos, customer counts, star ratings or awards, and do not add a logo wall. Where the design makes a checkable promise, such as a diagram drawn to scale or a figure tied to a trace, compute it and verify it in the DOM rather than eyeballing it."
    }
  ]
}
