# Deezer ARL cookie — for writing playlists (F12 → Application → Cookies → arl)
DEEZER_ARL=

# Spotify sp_dc cookie — for the Deezer → Spotify reverse flow (F12 → Application → Cookies → sp_dc)
# Optional; only needed for the reverse direction. Never commit the real value.
SPOTIFY_DC=
